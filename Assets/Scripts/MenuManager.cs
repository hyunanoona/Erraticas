using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuManager : MonoBehaviour
{

    /*
    Menú ּ ֶָ֢.
    ಄ Se encarga de leer las acciones del jugador en el menú y mostrar lo que él mismo indique. Ajustes, información, tutorial…
    ಄ Se comunica con el GameManager para el reinicio del juego, volver al menú inicial, menú de pausa…
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
